apiVersion: apps/v1
kind: Deployment
metadata:
  name: movie-api-deployment
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: movie-api
          env:
            - name: NODE_ENV
              value: staging
